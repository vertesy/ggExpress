% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggExpress.R
\name{qqqSummarize_mean_median}
\alias{qqqSummarize_mean_median}
\title{Summarize Mean and Median per Group}
\usage{
qqqSummarize_mean_median(df_XYcol_or_list, x = 1, y = 2)
}
\arguments{
\item{df_XYcol_or_list}{A data structure to summarize. Can be either:
\itemize{
\item A named list of numeric vectors (each element = one group), or
\item A data frame in long format with two columns: group and value.
}}

\item{x}{The index of the group column in the data frame (if applicable). Default: 1.}

\item{y}{The index of the value column in the data frame (if applicable). Default: 2.#'}
}
\value{
A data frame with three columns:
\itemize{
\item \code{group}: group identifiers,
\item \code{mean}: group mean values, and
\item \code{median}: group median values.
}
}
\description{
Converts either a list or a long-format data frame to a two-column data frame (group, value) and
calculates the mean and median for each group. This helper is compatible with ggplot-style input
and uses \code{ggExpress::qqqList.2.DF.ggplot()} to normalize list input.
}
\details{
The function unifies both input types into a consistent long-format data frame before summarizing.
The output contains one row per unique group, with corresponding mean and median values.
}
\examples{
# Example with a named list
lst <- list(A = 1:3, B = 4:6)
summarize_mean_median(lst)

# Example with a long-format data frame
df <- data.frame(group = rep(c("A", "B"), each = 3), value = 1:6)
summarize_mean_median(df)

}
