% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggExpress.R
\name{qmosaic}
\alias{qmosaic}
\title{Draw and Save a Doubledecker Mosaic Plot}
\usage{
qmosaic(
  df,
  x,
  y,
  weight = NULL,
  ext = MarkdownHelpers::ww.set.file.extension(default = "png", also_pdf = TRUE),
  also.pdf = TRUE,
  save.obj = FALSE,
  plot = TRUE,
  save = TRUE,
  plotname = paste("Mosaic plot:", x, "vs", y),
  subtitle = NULL,
  caption = NULL,
  filename = NULL,
  mdlink = MarkdownHelpers::unless.specified("b.mdlink", def = FALSE),
  palette_use = c("Set2", "Dark2", "Paired", "Pastel1", "Accent", "Set3", "Spectral",
    "auto")[1],
  w = 6,
  h = 5,
  limitsize = FALSE,
  ...
)
}
\arguments{
\item{df}{A data frame containing the variables to plot. Must include the X and Y columns.}

\item{x}{The column name (string) for the X-axis variable.}

\item{y}{The column name (string) for the Y-axis variable used for fill color.}

\item{weight}{Optional column name (string) specifying weights or frequencies.
If \code{NULL}, the function looks for a "Freq" column or assumes equal weights.
Default: \code{NULL}.}

\item{ext}{File extension for output image. Default: set via
\code{MarkdownHelpers::ww.set.file.extension(default = "png", also_pdf = TRUE)}.}

\item{also.pdf}{Save plot in both PNG and PDF formats. Default: \code{TRUE}.}

\item{save.obj}{Save the ggplot object as an \code{.RDS} file. Default: \code{FALSE}.}

\item{plot}{Display the plot after creation. Default: \code{TRUE}.}

\item{save}{Save the plot to file. Default: \code{TRUE}.}

\item{plotname}{Title of the plot and base for the filename. Default: \code{"Mosaic plot: x vs y"}.}

\item{subtitle}{Optional subtitle text. Default: \code{NULL}.}

\item{caption}{Optional text to display at the bottom right of the plot. Default: \code{NULL}.}

\item{filename}{Optional manual filename override. Default: \code{NULL}.}

\item{mdlink}{Insert a Markdown image link in reports. Default: read from
\code{"b.mdlink"} global variable using \code{MarkdownHelpers::unless.specified()}.}

\item{palette_use}{Color palette name. Supported: RColorBrewer palettes
(\code{"Set2"}, \code{"Dark2"}, \code{"Paired"}, \code{"Pastel1"}, \code{"Accent"}, \code{"Set3"}, \code{"Spectral"})
or \code{"auto"} for automatic hues. Default: \code{"Set2"}.}

\item{w}{Width of the saved plot (in inches). Default: \code{6}.}

\item{h}{Height of the saved plot (in inches). Default: \code{5}.}

\item{limitsize}{Passed to \code{ggsave()} to limit image size. Default: \code{FALSE}.}

\item{...}{Additional arguments passed to \code{ggmosaic::geom_mosaic()}.}
}
\value{
A \code{ggplot} object representing the mosaic plot.
}
\description{
Creates a mosaic-style (doubledecker) stacked bar plot where the X-axis bar width is proportional
to the group size and the Y-axis variable determines the color (fill). This function wraps
\code{ggmosaic::geom_mosaic()} in the style of ggExpress plotting wrappers and supports automatic file
saving, Markdown linking, and default color palettes.
}
\examples{
df <- data.frame(
  Group = rep(c("A", "B", "C"), each = 50),
  Outcome = sample(c("Yes", "No"), 150, replace = TRUE, prob = c(0.6, 0.4))
)
qmosaic(df = df, x = "Group", y = "Outcome")
}
