% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggExpress.R
\name{qheatmap}
\alias{qheatmap}
\title{Quick Heatmap Plot}
\usage{
qheatmap(
  data_matrix,
  plotname = FixPlotName("Heatmap of", substitute(data_matrix)),
  subtitle = NULL,
  suffix = NULL,
  caption = "caption",
  filename = NULL,
  also.pdf = FALSE,
  save.obj = FALSE,
  ext = MarkdownHelpers::ww.set.file.extension(default = "png", also_pdf = also.pdf),
  save = TRUE,
  mdlink = MarkdownHelpers::unless.specified("b.mdlink", def = FALSE),
  colors = (grDevices::colorRampPalette(c("#313695", "#FFFFFF", "#A50026")))(256),
  legend_title = "Intensity",
  scale = c("none", "row", "column"),
  cluster_rows = TRUE,
  cluster_cols = FALSE,
  row_annotation = NULL,
  col_annotation = NULL,
  row_annot_colors = NULL,
  col_annot_colors = NULL,
  plot = TRUE,
  xlab = "x axis",
  ylab = "y axis",
  w = 7,
  h = 6,
  ...
)
}
\arguments{
\item{data_matrix}{The data matrix to be plotted; must be either a matrix or a data frame.
Default: None (must be provided).}

\item{plotname}{The base name for the plot file. Default: "heatmap".}

\item{subtitle}{The subtitle for the heatmap. Default: "NULL" (indicating no subtitle).}

\item{suffix}{An optional suffix to append to the plot filename. Default: NULL.}

\item{caption}{The caption for the heatmap. Default: "mousse".}

\item{filename}{The specific filename to save the plot as; if NULL, a name is generated based on
\code{plotname} and other parameters. Default: NULL.}

\item{also.pdf}{Logical; indicates whether to also save the plot as a PDF. Default: FALSE.}

\item{ext}{The file extension for the saved plot image, adjusted based on \code{also.pdf}.
Default: "png".}

\item{save}{Logical; indicates whether to save the plot to file. Default: TRUE.}

\item{mdlink}{Logical; if TRUE and \code{save} is also TRUE, generates a markdown link for the saved
plot image. Default: FALSE, using \code{MarkdownHelpers::unless.specified}.}

\item{scale}{Character; indicates how to scale the data ("none", "row", or "column").
Default: "row".}

\item{cluster_rows}{Logical; indicates whether to cluster rows. Default: FALSE.}

\item{cluster_cols}{Logical; indicates whether to cluster columns. Default: FALSE.}

\item{xlab}{The global label for the x-axis; this might not display due to limitations.
Default: "Global X Label".}

\item{ylab}{The global label for the y-axis; this might not display due to limitations.
Default: "Global Y Label".}

\item{w}{Width of the saved plot image, in inches. Default: 10.}

\item{h}{Height of the saved plot image, in inches. Default: 8.}

\item{...}{Additional arguments passed to \code{ggheatmap}.}

\item{title}{The main title for the heatmap. Default: "Heatmap".}

\item{color}{The color palette for the heatmap values, using a blue-white-yellow gradient.
Default: colorRampPalette(c( "#0073c2","white","#efc000"))(100).}

\item{legendName}{The title of the heatmap legend. Default: "Intensity".}

\item{annotation_rows}{Data frame with row annotations. Default: NULL.}

\item{annotation_cols}{Data frame with column annotations. Default: NULL.}

\item{annotation_color}{List of colors for the annotations. Default: NULL.}
}
\value{
A \code{ggplot} object representing the heatmap, conditionally modified based on the provided
parameters.
}
\description{
Generates a heatmap plot using the \code{ggheatmap} package with features such as automatic
file saving and markdown link generation. This function simplifies the process of creating and
customizing heatmaps, with support for clustering, annotations, and scaling.
}
\examples{
\dontrun{
# Create example matrix
mat <- as.matrix(mtcars)

# Row annotations: cyl and gear
row_annot <- mtcars[, c("cyl", "gear")]

# Column annotations: arbitrary grouping
col_annot <- data.frame(
  group = rep(c("A", "B"), length.out = ncol(mat)),
  stringsAsFactors = FALSE
)

# Example 1: Basic heatmap with default settings
qheatmap2_(
  mat,
  plotname = "mtcars basic heatmap",
  save = FALSE
)

# Example 2: Heatmap scaled by column
qheatmap(
  mat,
  plotname = "mtcars heatmap scaled by column",
  scale = "column",
  save = FALSE
)

# Example 3: Heatmap with row and column annotations
qheatmap(
  mat,
  plotname = "mtcars heatmap with annotations",
  scale = "column",
  row_annotation = row_annot,
  col_annotation = col_annot,
  save = FALSE
)
}

}
