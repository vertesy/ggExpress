% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggExpress.R
\name{qqqAnnotateTopLabels}
\alias{qqqAnnotateTopLabels}
\title{Annotate Top Labels on a ggplot}
\usage{
qqqAnnotateTopLabels(df, x, y, labels = NULL, fun = mean, digits = 2)
}
\arguments{
\item{df}{A data frame containing the data to be plotted.}

\item{x}{Character string. Column name mapped to the x-axis.}

\item{y}{Character string. Numeric column name mapped to the y-axis.}

\item{labels}{Optional character or numeric vector of labels, one per x-category.
If NULL, labels are computed using \code{fun}.}

\item{fun}{Summary function used to compute labels when \code{labels} is NULL.
Default is \code{mean}.}

\item{digits}{Number of digits used to round computed labels. Default is 2.}
}
\value{
A ggplot2 layer (\code{geom_text}) to be added to a ggplot object.
}
\description{
Creates a ggplot2 text layer that annotates one label per x-category
at the top of the plotting panel. Labels can be computed from the data (default)
or provided explicitly.
}
\examples{
p + qqqAnnotateTopLabels(df, x = "dose", y = "len")
p + qqqAnnotateTopLabels(df, x = "dose", y = "len", labels = c("A", "B", "C"))

}
