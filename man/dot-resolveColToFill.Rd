% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggExpress.R
\name{.resolveColToFill}
\alias{.resolveColToFill}
\title{Resolve the col argument to a ggpubr fill value}
\usage{
.resolveColToFill(col, vars, palette_use)
}
\arguments{
\item{col}{The \code{col} argument as supplied by the caller. Must not be NULL.}

\item{vars}{Column names of the data frame being plotted.}

\item{palette_use}{The palette in use, passed through from the caller.}
}
\value{
A single string: either a column name for ggpubr to map, or a literal color.
}
\description{
Shared \code{col} resolution for the data frame / list based plotting functions
(\code{qboxplot}, \code{qviolin}, \code{qstripchart}). \code{col} may be:
(1) the name of a column to color (group) by, which is returned unchanged so ggpubr maps
it through \code{palette_use};
(2) a single palette index - a positive whole number, or a digit-only string such as
\code{"2"} - which is looked up in \code{palette_use} and returned as a literal color;
(3) any other value, returned unchanged and used by ggpubr as a literal color.
A numeric \code{col} is always a \emph{palette} index, never a column index, which keeps these
functions consistent with \code{qbarplot()} and \code{qhistogram()}.
}
\examples{
ggExpress:::.resolveColToFill("dose", c("dose", "len"), "jco") # -> "dose"
ggExpress:::.resolveColToFill(2, c("dose", "len"), "jco") # -> 2nd palette color
ggExpress:::.resolveColToFill("red", c("dose", "len"), "jco") # -> "red"
}
